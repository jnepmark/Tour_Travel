import React, { useState, useEffect } from 'react';

const Hero = () => {
  return (
    <div className="bg-black/10 h-full">
      <div className="h-full flex justify-center items-center p-4 bg-primary/10">
        <div className="container grid grid-cols-1 gap-4 text-white">
          {/* Your content without destinations and search button */}
        </div>
      </div>
    </div>
  );
};

export default Hero;